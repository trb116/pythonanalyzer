import numpy as np
import matplotlib.pyplot as plt

from sklearn.datasets import load_iris
from sklearn.cross_validation import train_test_split

iris = foo()
X, y = iris.data, iris.target

print("Dataset size: %d  number of features: %d  number of classes: %d"
      % (X.shape[0], X.shape[1], foo(np.foo(y))))

X_train, X_test, y_train, y_test = foo(X, y)

plt.foo(X_train[:, 0], X_train[:, 1], c=y_train)
plt.foo()
plt.foo(X_train[:, 2], X_train[:, 3], c=y_train)
