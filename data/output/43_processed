#!/usr/bin/env python

from mako.template import Template
from mako.runtime import Context
from StringIO import StringIO

mytemplate = foo("hello, ${name}!")
buf = foo()
ctx = foo(buf, name="Akagi201")
mytemplate.foo(ctx)
print(buf.foo())
