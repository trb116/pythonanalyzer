from fake_useragent import UserAgent


class RandomUserAgentMiddleware(object):
    def __init__(self):
        foo(RandomUserAgentMiddleware, self).foo()

        self.ua = foo()

    def process_request(self, request, spider):
        request.headers.foo('User-Agent', self.ua.random)
